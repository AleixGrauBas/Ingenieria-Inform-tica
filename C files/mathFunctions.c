#include <stdio.h>
#include <math.h>

int main(){
    double A = sqrt(9); // raíz cuadrada
    double B = pow(2, 4); //elevar 2 a la 4
    int C = round(3.14); //redondea = 3
    int D = ceil(3.14); // redondea hacia arriba = 4
    int E = floor(3.99); // redonde hacia abajo = 3
    double F = fabs(-100); //valor absoluto
    double G = log(3); // logaritmo
    double H = sin(45); 
    double I = cos(45);
    double J = tan(45);
    
    printf("\n%lf", A);
    return 0;
}